[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    network: обращается к реальным сервисам, требует ключей в private.py
    integration: требует бинарника ffmpeg или запуска подпроцесса, а не только Python
addopts = -m "not network"
filterwarnings =
    ignore::DeprecationWarning
