# Скопируйте этот файл в private.py и заполните значения.
# private.py в репозиторий не коммитится.

discord_bot_token = ''

# Spotify: плейлисты требуют OAuth, регистрация приложения на developer.spotify.com
spotify_client_id = ''
spotify_client_secret = ''
spotify_redirect_uri = 'http://127.0.0.1:3000/callback'

# Yandex Music: OAuth-токен аккаунта
yandex_token = ''

# SoundCloud: нужен только для треков за пейволлом SC Go+
soundcloud_oauth_token = ''

# VK Музыка: путь к cookies.txt залогиненного аккаунта, выгружается расширением
# из браузера. Без него провайдер выключен: VK отдаёт неавторизованным 31
# секунду при полной заявленной длительности и никак это не помечает.
# Сессия привязана к адресу входа: куки, выданные одному IP, с другого не
# работают. Живёт около 180 дней
vk_cookies = ''

# VK Музыка: маршрут для трафика VK, необязателен. Пусто — бот ходит в VK
# своим обычным маршрутом. Задан — при прогреве поднимается локальный прокси
# (tools/vk_proxy.py) и через него идут и API, и поток: ссылка подписана
# адресом запросившего, разные выходы дают 403.
# Нужен, если бот сидит в VPN, а куки выданы другому адресу
vk_proxy = ''

# префикс текстовых команд. Пустая строка безопасна: client.py в этом случае
# берёт commands.when_mentioned, и командой считается только упоминание бота
prefix = '!'
